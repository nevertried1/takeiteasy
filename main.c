#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int num = 10;
	if (num >= 7);
	printf("Number is greather or equal 10.\n");
	if(num<15);
	printf("Number is less than 15.\n ");

return 0;
}
